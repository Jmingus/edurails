<p id="notice"><%= notice %></p>

<table class="table">

  <thead>
	  <tr>
	    <th>Company</th>
	    <th>Locations</th>
	  </tr>
  </thead>

  <tbody>
	  <tr>
	    <td> <%= @course.location.company.name %> </td>
	    <td> <%= @course.location.name %> </td>
	  </tr>
  </tbody>

</table>


<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
